{
  "name": "followupdose",
  "version": "1.0.0",
  "description": "مُذكِّر أدوية الأطفال - Baby Medicine Reminder",
  "scripts": {
    "cap:sync": "npx cap sync android",
    "build:android": "npx cap sync android && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.0",
    "@capacitor/core": "^6.1.0",
    "@capacitor/local-notifications": "^6.0.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0"
  }
}
